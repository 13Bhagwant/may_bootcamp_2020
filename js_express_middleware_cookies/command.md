# Commands

```bash
# To create a project
npm init -y

# Add express to our project
npm i --save express

# Installing nodemon
npm i --save-dev nodemon

# Installing morgan
npm i --save morgan

# Adding ejs (template engine)
npm i --save ejs
```